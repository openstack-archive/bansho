'use strict';

angular.module('bansho.surveil')
    .factory('surveilQuery', ['fields', 'filters', 'paging', 'timeInterval',
        function(fields, filters, paging, timeInterval) {
            



    }]);
