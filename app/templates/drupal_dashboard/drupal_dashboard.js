"use strict";

angular.module("bansho.view.drupalDashboard", [])

    .controller("DrupalDashboardViewCtrl", ["$scope", "$routeParams",
        function ($scope, $routeParams) {
            if ($routeParams.id) {

            }   
        }]);
